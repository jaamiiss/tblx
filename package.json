{
  "name": "tblx",
  "version": "3.1.0",
  "description": "The Blacklist - Zombified",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "validate": "node scripts/validate-data.js",
    "build:watch": "nodemon --watch src --ext js,css --exec 'node build.js'",
    "start:prod": "pnpm run build && node server.js",
    "vercel-build": "pnpm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.8.1",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.10",
    "express": "^5.2.1"
  },
  "pnpm": {
    "overrides": {
      "qs": "^6.14.1"
    }
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "clean-css": "^5.3.3",
    "nodemon": "^3.1.11",
    "terser": "^5.44.1"
  }
}