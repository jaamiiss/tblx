{
  "name": "test-htmx",
  "version": "2.7.0",
  "description": "The Blacklist",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "node build.js",
    "build:watch": "nodemon --watch src --ext js,css --exec 'node build.js'",
    "start:prod": "npm run build && node server.js",
    "vercel-build": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.9",
    "express": "^4.19.2",
    "firebase-admin": "^13.5.0",
    "memory-cache": "^0.2.0"
  },
  "devDependencies": {
    "clean-css": "^5.3.3",
    "nodemon": "^3.1.4",
    "terser": "^5.44.0"
  }
}
